using System;
using System.Linq;
using System.Collections.Generic;
using mathsOne;
using CollectionsProj;
namespace csharp_code
{
    class Program
    {
        static void Main(string[] args)
        {
          
          // ****************************** Test For Collections *************************************/ 
          // gathering the collections results and sorting ou the data from lowest to highest
          /* double listTime = CollectionsOne.ReturnListTime(200);
          double stackTime = CollectionsOne.ReturnStackTime(200);
          double queueTime = CollectionsOne.ReturnQueueTime(200);
          double dictTime = CollectionsOne.ReturnDictTime(200);
          double[] timeList = {queueTime,stackTime,listTime,dictTime};
          List<double> timeLister = new List<double>();
          for(int i = 0; i < timeList.Length; i ++)
          {
            Console.WriteLine($"time from collections are = {timeList[i]}");
             timeLister.Add(timeList[i]);
          }
          timeLister.Sort();
          foreach(var i in timeLister)
          {
            Console.WriteLine($"Time in order is = {i}");
          }*/



        }
    }
}
